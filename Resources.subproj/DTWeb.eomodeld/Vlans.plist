{
    attributes = (
        {
            columnName = "C_VLAN"; 
            externalType = VARCHAR2; 
            name = cVlan; 
            valueClassName = NSString; 
            valueType = S; 
            width = 5; 
        }, 
        {
            columnName = "LISTE_IP"; 
            externalType = VARCHAR2; 
            name = listeIp; 
            valueClassName = NSString; 
            valueType = S; 
            width = 50; 
        }, 
        {
            columnName = "LL_VLAN"; 
            externalType = VARCHAR2; 
            name = llVlan; 
            valueClassName = NSString; 
            valueType = S; 
            width = 20; 
        }, 
        {
            columnName = PRIORITE; 
            externalType = NUMBER; 
            name = priorite; 
            valueClassName = NSNumber; 
            valueType = i; 
        }, 
        {
            columnName = "PRISE_COMPTE"; 
            externalType = VARCHAR2; 
            name = priseCompte; 
            valueClassName = NSString; 
            valueType = S; 
            width = 1; 
        }
    ); 
    attributesUsedForLocking = (cVlan, listeIp, llVlan, priorite, priseCompte); 
    className = "org.cocktail.dt.server.metier.EOVLans"; 
    classProperties = (cVlan, listeIp, llVlan, priorite, priseCompte); 
    externalName = "GRHUM.VLANS"; 
    fetchSpecificationDictionary = {}; 
    internalInfo = {"_nameInObjectStore" = VLANS; }; 
    name = Vlans; 
    primaryKeyAttributes = (cVlan); 
}
